# Findings ledger (Round 1 audit, 2026-07-14, CONSOLIDATED)

Schema: `id | ours | standard (citation) | location | severity | disposition | rationale`
Dispositions: rename / keep+trade-off / slop. Cross-lane duplicates merged; original per-pass rows
in git history of this file. Companion docs: `2026-07-14-salvage-inventory.md` (Round-3 spec.md
sources), `2026-07-14-architecture-inventory.md` (tree ground truth).

## Totals

| lane | rows after merge | high | low |
|---|---|---|---|
| 1 c_model naming | 7 | 2 | 5 |
| 2 SV/specgen/build | 3 | 1 | 2 |
| 3 docs stale claims | 2 clusters (26 claims) | — | — |
| 4 packaging/provenance | 16 | 9 | 7 |

## Gate outcome (2026-07-14, user-decided; Codex AGREE x4 on D4/D5/D7/D8)

| # | decision |
|---|----------|
| D1 | fully **master/slave** — rename the scattered manager/subordinate PROSE to master/slave; identifiers stay; "Network Master Unit" fullnames now correct; AXI modports stay |
| D2/D3 | **no coined term** — RZ1 tag deleted; mechanism described as "deterministic VC allocation: same (dst,id) → same VC, per-hop VA stage skipped"; `pinned` → plain descriptive name (Round-2 plan picks it) |
| D4 | approved — `landing_` → input register; NoC link modports `mosi/miso` → upstream/downstream |
| D5 | approved — (a) ATTRIBUTION/LICENSE files may name upstream (legal exception); (b) informal ported-from comments stay + formal per-file tag + ATTRIBUTION.md mirroring axi/ treatment |
| D6 | approved — (a) delete NSU `use_pools_` scalar branch (mirror NMU size-1 pool); (b) regen noc_types_pkg_vc* banners |
| D7 | ratified — keep+trade-off set becomes trade-off.md sections |
| D8 | option (i) — modified-flag note in sim/dv/README provenance table; the 2-line edit stays (emit_result_csv.py consumes N) |

## Gate decisions (original agenda, kept for record)

| # | decision | context |
|---|----------|---------|
| D1 | **AXI role vocabulary direction**: manager/subordinate (IHI 0022 issue H, which specgen already cites) or master/slave (pre-H AXI4, dominant industry spelling)? Scope decided per surface class (table below). | pass-1 agents argued toward manager/subordinate; a pass-2 agent argued AXI4 prose should say master. Today the repo MIXES both — the only wrong answer is the current mix. Round-2 plan will carry the exact old→new symbol map per file. |
| D2 | **`pinned`/`VC pin` → binding?** | your prior direction says binding; two independent auditors found the authority itself uses "pin" (cheatsheet.md:65, concepts.md "Pinning ordering to..."). Either is defensible — confirm binding or accept pin as sanctioned. |
| D3 | **`RZ1` → exact term "static VC assignment"** | seed finding, confirmed high; occurrences are comments+tests only (nsu/vc_arbiter.hpp:9,130; test_nsu_vc_arbiter.cpp:83) plus docs. One term, applied verbatim. Recommend approve. |
| D4 | **Low-severity renames batch**: `landing_`→input register; `modports ["master","slave"]` + `mosi/miso`→per D1; NMU/NSU json fullnames→per D1 | approve as batch or cherry-pick. (WORMHOLE_ARBITRATION removed from this batch — reclassified keep+trade-off after Codex code check, see L2-003.) |
| D5 | **Legal-attribution exception** to the "no external IP names in code/docs" rule — two sub-questions: (a) formal ATTRIBUTION/LICENSE files may name the upstream (recommend yes, legally load-bearing); (b) may source comments keep the existing informal "Ported from floo_*.sv" file references, or must all upstream naming move into ATTRIBUTION? | informal refs already exist throughout router.hpp/meta_buffer.hpp/rob.hpp; they are traceability aids. Recommend (a) yes, (b) keep comments, add formal files. |
| D6 | **Round-2 pull-ins from backlog**: (a) NSU `use_pools_` mirror to size-1 pool (row L1-007); (b) regen `noc_types_pkg_vc*` to refresh stale banners (body verified current by --check; banner-only fix) | (a) real code decision; (b) trivial regen. Recommend both in. |
| D7 | **Ratify keep+trade-off set** (L1-004/005, L2-002, L2-003, L4 deliberate-absent rows) | each becomes a trade-off.md section; no code change. |
| D8 | **`axi_bw_monitor.sv` vendored-edit resolution — pick one**: (i) manifest-note (add modified-flag to sim/dv/README provenance table), (ii) patch-overlay (restore verbatim file + carry the 2-line $display edit as a patch/wrapper), (iii) revert the edit (lose the latency-N output emit_result_csv.py consumes) | (i) is cheapest and honest; (iii) breaks a consumer; (ii) is the trust-upstream-defaults ideal. |

D1 per-surface execution table (scope decided per class, not globally):

| surface class | examples | lean |
|---|---|---|
| AXI protocol role in prose/comments/JSON fullnames | "AXI4 manager transport", "Network Master Unit", modports | rename to the chosen pair (cheap, no ABI) |
| AXI role class names + feature ids | `AxiMasterPort`, `AxiSlavePort`, `FEAT-*-AXI_SLAVE/MASTER_PORT` | per D1 scope choice (medium churn, c_model only) |
| endpoint-side bus labels in generated SV | `master_axi_req`/`slave_axi_req` (gen_tb_top emissions; note: `master_axi_req_i` sits on the NMU's AXI-subordinate face — the label is the ENDPOINT's role, not the NI's) | keep+trade-off lean (heavy churn through generated SV + hand-written consumers) |
| NoC credit-depth param | `SLAVE_VC_BUFFER_DEPTH` (receiver credit depth, an AXI role name on a NoC-layer symbol) | keep+trade-off lean (load-bearing across generated C++/SV) |
| NoC link modports | `mosi`/`miso` | rename to upstream/downstream (small, source JSON only) |

## Lane 1: c_model naming

| id | ours | standard (citation) | location | severity | disposition | rationale |
|---|---|---|---|---|---|---|
| L1-001 | `RZ1` codename for the return-path static VC map | static/deterministic VC allocation (glossary "VC allocator (VA)", Ch 6; noc-ordering "inter-packet co-location") | nsu/vc_arbiter.hpp:12,132; test_nsu_vc_arbiter.cpp; docs refs | high | slop→rename | bespoke codename, zero industry meaning; name the mechanism [D3] |
| L1-002 | `pinned` / "VC pin" for statically-mapped VC state | VC binding (user direction) [counter-evidence x2: cheatsheet.md:65 "pin X-Y→VC0"; concepts.md "Pinning ordering to 'the same VC identifier'"] | nmu/vc_arbiter.hpp:16,111-153,200-211; nsu/vc_arbiter.hpp:129-132 | low | rename | [D2] |
| L1-003 | mixed AXI role vocabulary: `AxiMaster*`/`AxiSlave*`/`axi_master_port` identifiers vs "manager"/"subordinate" prose ("AXI4 manager transport" self-contradiction); NiPath comments | ONE consistent pair per IHI 0022 (pre-H: Master/Slave; issue H: Manager/Subordinate) | nsu/axi_master_port.hpp; nsu/meta_buffer.hpp; nsu/depacketize.hpp; nsu/packetize.hpp; nsu/port_params.hpp; nmu/axi_slave_port.hpp; ni/ni_stage.hpp:11-14; wrap/nsu_wrap.hpp vs nmu_wrap.hpp; tests/common | high | rename (direction per D1) | umbrella row for ALL lane-1 master/slave findings |
| L1-004 | `VcArbiter` (both NI sides): selection half is textbook VC allocation, drain half is true N→1 arbitration | glossary separates VC allocator (VA) from arbiter (Ch 6) | nmu/vc_arbiter.hpp:49,223-238; nsu/vc_arbiter.hpp:38 | low | keep+trade-off | mixed-role block matching the upstream RTL counterpart's naming; trade-off documents the VA/arbiter duality [D7] |
| L1-005 | `MetaBuffer` family (class, wrap params, specgen keys `META_BUFFER_*`/`FEAT-NSU-META_BUFFER`, generated tb comments) | TSHR — Transaction Status Holding Registers (glossary MSHR/TSHR, Ch 2) | nsu/meta_buffer.hpp:45; wrap/nsu_wrap.hpp:52-53,70-71; specgen/source/noc_function_blocks.json:109; constants.yaml:185-201 | low | keep+trade-off | deliberate 1:1 port of the upstream RTL meta buffer for parity traceability; trade-off documents the TSHR mapping [D7] |
| L1-006 | `landing_` / "stage-1 input landing register" | input register / buffer-write (BW) stage of an input-buffered router (glossary, Ch 6) | router/router.hpp:167-168,199,282; ni/pipeline_stage.hpp:8 | low | rename | non-industry metaphor [D4] |
| L1-007 | NSU `VcArbiter` retains the `use_pools_` scalar-VC branch the NMU collapsed into a size-1 pool | mirror the NMU shape (single pool code path) | nsu/vc_arbiter.hpp:58,97,107,152 vs nmu/vc_arbiter.hpp:61-65,102-103 | low | rename — Round-2 code change [D6a] | backlog debt promoted to ledger row per Codex review (structural asymmetry, not naming) |

## Lane 2: SV / specgen / build / config

| id | ours | standard (citation) | location | severity | disposition | rationale |
|---|---|---|---|---|---|---|
| L2-001 | AXI role vocabulary across generated+source surfaces: `master_axi_req`/`slave_axi_req` (fabric, wraps, gen_tb_top emissions), `SLAVE_VC_BUFFER_DEPTH` (constants.yaml→pkg), NMU/NSU fullnames "Network Master/Slave Unit", `FEAT-*-AXI_SLAVE/MASTER_PORT` ids, `modports ["master","slave"]`, `mosi/miso` link modports, mixed manager/subordinate prose | Manager/Subordinate (IHI 0022H, cited by specgen itself) or consistent master/slave; `mosi/miso` → upstream/downstream (NoC layer) | specgen/source/{noc_function_blocks.json:14,18,87,91,144; interface_handshake.json:13,24-31; constants.yaml:64-72}; sim/tools/gen_tb_top.py:220-223,605-608; src/sv/*.sv | high (mix) / low (each) | per D1: prose+json fullnames+modports lean rename; load-bearing identifiers lean keep+trade-off | one decision governs; per-surface split at D1 |
| L2-002 | `FEAT-ROUTER-VC_ARBITRATION` ("per-output flit-level round-robin across VCs, credit-gated") | switch allocation (SA) — grants output ports to input VCs per flit (glossary, Ch 6) | specgen/source/noc_function_blocks.json:144 | low | keep+trade-off | mechanism is SA; name kept as feature-id stability, trade-off documents the SA mapping [D7] |
| L2-003 | `FEAT-ROUTER-WORMHOLE_ARBITRATION` ("per-(output,vc) packet lock head→tail") | wormhole flow control's switch-level output lock (SA held head→tail, Ch 5/6); NOT VA — the design is fixed-VC (vc_id stamped at injection, router.hpp:218,225,267-268 consumes it; no VA stage exists by design) | specgen/source/noc_function_blocks.json:143 | low | keep+trade-off | REVISED after Codex code check: pass-2 auditor's VA reading was wrong for this fixed-VC design; name matches the mechanism and its upstream RTL counterpart; trade-off documents the SA-hold mapping [D7] |

## Lane 3: docs stale claims

All 26 claims verified against the tree. Grouped by whether the containing file SURVIVES Round 3.

### L3-001 — surviving files (must be fixed in Round 3)

| file | stale claims |
|---|---|
| CLAUDE.md | no-router-class claim; xy_route routing; MasterWrap/SlaveWrap + cmodel_master/slave create; "Config: YAML (c_model/config/), no JSON"; ChannelModel as co-sim datapath; `c_model/...` path prefix (4 lines); `docs/_archive/` dangling pointer |
| README.md | "Where code lives" dead map (c_model/, tools/, sim/test_patterns/); sim/test_patterns/README.md dangling ref; `make run-tb-top`; license section vs LICENSE contradiction; CMake 3.20 vs Makefile 3.14 |
| LICENSE | third-party pointer `c_model/include/axi/ATTRIBUTION.md` dead path; (via L4) missing sim/dv packages |
| sim/dv/README.md | "verbatim, do not edit" vs locally-modified axi_bw_monitor.sv |
| src/c_model/include/axi/ATTRIBUTION.md | "no project-wide license selected yet" contradicts root LICENSE |

### L3-002 — dying docs (die with deletion; recorded so spec.md does not inherit them)

| doc | claim class |
|---|---|
| architecture.md | no-router-class; four-wraps/master-slave ticks; sim/c path; ChannelModel-as-hop; §3.3 multi_candidate/vc_mode (deleted mechanisms); route_par zero-fill detail; unresolvable external RTL citations; EXC-scenarios claim; c_model paths |
| development.md | AX4-CAT scenario convention; run-tb-top (4 locations); sim/c path; c_model paths |
| verification-environment.md | "imported unmodified"; import list omits common_cells |
| cosim-log.md | "imported unmodified" |
| nmu-rob-microarchitecture.md | write_rob_mode/mode_w_ (removed by 281a6d4); "B-only RoB unreachable"; backlog "FIXED 2026-07-11" dangling ref |
| performance-probe.md | run-tb-top entry; "2-node testbench"; §2/§4 AXI Profile metrics UNBUILT; §5 Trace view UNBUILT |
| 2026-07-02-pulp-axi-vip-node-design.md | whole-doc stale (CR axis retired 2026-07-13; reorder_compare absent; scoreboard re-adopted) |
| docs/issue/ARCHITECTURE.md | one reorg behind throughout (§2 root tree, §3.3 line counts, §4 file list, §5 AX4 tree, §6.2) |
| docs/slides/genamba-role1-port-SLIDES.md | describes retired tb-genamba harness |

## Lane 4: packaging / provenance

| id | ours | standard | location | severity | disposition |
|---|---|---|---|---|---|
| L4-001 | README+ATTRIBUTION.md "no license selected" vs proprietary root LICENSE (3-file contradiction) | license copy states what LICENSE carries (FlooNoC, pulp axi) | README.md:89-97; axi/ATTRIBUTION.md; LICENSE | high | slop — reconcile in Round 3 |
| L4-002 | LICENSE third-party pointer dead path (`c_model/...`) | legal notice resolves to real path (OpenTitan) | LICENSE:12-15 | high | rename (one line) |
| L4-003 | root third-party notice missing the four sim/dv Solderpad packages | root notice aggregates every vendored component (OpenTitan; pulp Bender.yml) | README/LICENSE vs sim/dv/README.md | high | rename |
| L4-004 | sim/dv "verbatim, do not edit" manifest vs locally-modified axi_bw_monitor.sv | patched vendored files flagged at manifest level (lowRISC vendor patches; Apache-2.0 §4) | sim/dv/README.md vs axi_bw_monitor.sv:8-9 | high | rename (modified-note or revert; pairs with backlog DV-debt) |
| L4-005 | FlooNoC-derived C++ ports lack FORMAL attribution/license treatment (informal "Ported from floo_*.sv" refs exist throughout; the sibling axi/ ports have per-file tag + ATTRIBUTION.md) | formal ATTRIBUTION.md + per-file license tag mirroring the axi/ treatment (Solderpad notice preservation) | router/router.hpp:10; nsu/meta_buffer.hpp:20,35; nmu/rob.hpp:47,108,172,200,250-311 | high | rename [D5] |
| L4-006 | `noc_types_pkg_vc{1,2,4,8}.sv` stale Source-SHA BANNERS only (constants.yaml changed 26108ee; body verified current — codegen.py --check :238-242 covers these files but compares body, not banner) | provenance banner matches source (regen refreshes it) | specgen/generated/sv/noc_types_pkg_vc*.sv:4 | low | rename — Round-2: regen (banner refresh); optional banner-hash check [D6b]. REVISED after Codex: original "no drift coverage" claim was wrong |
| L4-007 | no known-limitations list | release doc enumerates limitations (FlooNoC) | fresh docs | high | rename — lands in spec.md |
| L4-008 | no verification summary (conformity result unstated) | verification state at top level (FlooNoC, pulp axi) | fresh docs | high | rename — lands in spec.md |
| L4-009 | platform matrix undeclared (WSL/RHEL/VCS-Linux realities live in Makefile comments) | host × flow × simulator table (FlooNoC, OpenTitan) | fresh README/spec | high | rename |
| L4-010 | no pinned python-requirements (PyYAML prose-only) | pinned manifest (OpenTitan) | repo root | low | rename |
| L4-011 | `.gitignore /docs/image/*.jpg` vs 6 tracked jpgs | tracked set ∩ ignore = ∅ | .gitignore | low | rename — drop the ignore line (user keeps docs/image) |
| L4-012 | generated-artifacts policy undeclared; gen_tb_top emissions carry no SHA/version and no --check | declare policy + regen entry (OpenTitan); banner convention already met for specgen | README + Makefile (`regen:` target) + gen_tb_top | low | rename |
| L4-013 | CMake 3.20 (README) vs 3.14 (Makefile comment) | one version floor | README vs Makefile | low | rename |
| L4-014 | specgen/ lacks its own README boundary declaration | per-directory README (pulp, OpenTitan) | specgen/ | low | rename |
| L4-015 | deliberate-absent set: badges/citation, CHANGELOG/version scheme, CONTRIBUTING, .editorconfig, SPDX headers (pending license reconcile), docs index (superseded by fresh doc set), commit `@ ` prefix history | — | — | low | keep+trade-off [D7] |
| L4-016 | README "Where code lives" dead-path map | accurate top-level map (FlooNoC) | README.md | high | slop — fixed by Round-3 README rewrite (merged with L3-001) |

## Audit log (condensed)

- Pass 1: 8 subagents (lanes 1a/1b/1c, 2a/2b, 3a/3b, 4). Findings: L1 x11 raw, L2 x14 raw, L3 salvage 49 + stale 28 raw, L4 x6.
- Passes 2-4 (lanes 1-2): lane 1 dry at pass 3-4; lane 2 dry at pass 3-4. New en route: manager-vs-master direction conflict (→D1), WORMHOLE_ARBITRATION=VA, mosi/miso.
- Passes 2-9 (lane 3): backlog.md missed in pass 1 (7 salvage rows); pulp-vip whole-doc stale (CR revert 2026-07-13); probe AXI/Trace machinery UNBUILT; specgen regen workflow salvage; build-portability salvage; dry at passes 8-9.
- Passes 2-14 (lane 4): license/provenance cluster excavated across passes 5-10 (third-party disclosure, LICENSE pointer, verbatim-vs-modified, FlooNoC-ports attribution); pass 12 first attempt returned zero-evidence DRY — discarded, redo caught noc_types stale banners; passes 13-14 evidence-based DRY.
- The `pinned` seed row: two independent auditors surfaced authority counter-evidence (cheatsheet "pin X-Y→VC0"; concepts "Pinning ordering to...") — D2.
- Verified-clean (no rows): RoB/rob_idx, SAM, wormhole/credit/flit/VC vocabulary, traffic-pattern names (booksim-standard), DPI naming, make targets, tb/topology pairing, PII/secrets/binaries, cpp+json generated artifacts (SHA-verified current).
- Process deviation record: lane 4 needed 14 passes; its pass-12 first attempt returned a zero-evidence DRY and was discarded+re-run (the redo caught the noc_types stale banners). Lane-4 passes 8+ ran under a high-severity-only convergence clause.
- Codex ledger review (Task 8): NOT SHIP → fixes applied: L2-003 reclassified (VA reading wrong for fixed-VC design; now keep+trade-off), L4-006 downgraded (banner-only stale; --check does cover body), L4-005 reworded (formal-treatment gap, informal refs exist), D1 per-surface table added, D3 fixed to exact term, D5 split into (a)/(b), L1-007 added for D6a, old D8 FYI moved here, new D8 = bw_monitor three-way fork. Round-2 symbol map noted as a Round-2-plan deliverable.
